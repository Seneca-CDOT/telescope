# Environment variables needed if you want to run `pnpm dev`
# and run the server locally, outside of Docker. For all the various
# ways we run this using Docker, the env is defined by one of the
# config/env.* files in the root.
FIRESTORE_EMULATOR_HOST=localhost:8088
USERS_PORT=7000
USERS_URL=http://localhost:7000
