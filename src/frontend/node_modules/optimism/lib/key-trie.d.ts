export declare class KeyTrie<K> {
    private weakness;
    private makeData;
    private weak?;
    private strong?;
    private data?;
    constructor(weakness: boolean, makeData?: (array: any[]) => K);
    lookup<T extends any[]>(...array: T): K;
    lookupArray<T extends IArguments | any[]>(array: T): K;
    private getChildTrie;
}
