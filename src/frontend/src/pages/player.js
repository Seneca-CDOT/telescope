import React from 'react';

import PageBase from './PageBase';
import PlayerPage from '../components/PlayerPage';

export default function Player() {
  return (
    <PageBase title="Player">
      <PlayerPage />
    </PageBase>
  );
}
