import React from 'react';

import PageBase from './PageBase';
import StreamsPage from '../components/StreamsPage';

export default function Streams() {
  return (
    <PageBase title="Streams">
      <StreamsPage />
    </PageBase>
  );
}
