os: 
  - linux
  - osx
  - windows
language: node_js
node_js:
  - "node"
  - "10"
  - "8"
  - "6"
# addons:
#   homebrew:
#     packages:
#      - redis
#     update: true


#manual install
    install:
  - if [ $TRAVIS_OS_NAME = osx ]; then
    brew update;
    brew install redis;
    redis-server /usr/local/etc/redis.conf;
    fi

# services:
#   - redis-server