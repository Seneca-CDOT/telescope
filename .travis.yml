os: 
  - linux
  - osx
  - windows
language: node_js
node_js:
  - "stable"
  install:
  - if [ $TRAVIS_OS_NAME = osx ]; then
    brew update;
    brew install redis;
    redis-server /usr/local/etc/redis.conf;
    fi
# addons:
#   homebrew:
#     packages:
#      - redis
#     update: true
# services:
#   - redis-server